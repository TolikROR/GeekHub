<h1>edit group</h1>

<%= form_for @group do |f| %>
  <p>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </p>
  <%= f.submit %>
<% end %>

<%= link_to 'Back', kafedra_groups_path(@group.kafedra) %>
