<h1> Edit university </h1>

<%= form_for @university do |f| %>
  <p>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </p>
  <%= f.submit %>
<% end %>

<%= link_to 'Back', city_universities_path(@university.city) %>
