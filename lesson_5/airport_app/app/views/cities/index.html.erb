<h1> Index Cities </h1>

<% @cities.each do |city| %>
  <%= city.name %>

  <p>
    <%= link_to 'Show', city %>
    <%= link_to 'Edit', edit_city_path(city) %>
    <%= link_to 'Destroy', city, method: :delete %><br>
  </p>
<% end %>

<%= link_to 'Back', root_path %>
